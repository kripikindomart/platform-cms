import { IsOptional, IsInt, Min, IsString, IsIn } from 'class-validator';
import { Type } from 'class-transformer';
import { ApiProperty } from '@nestjs/swagger';

/**
 * Query {{displayName}} DTO
 * For pagination, filtering, and sorting
 * 
 * @generated
 */
export class Query{{capitalize className}}Dto {
  @ApiProperty({ required: false, default: 1 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @ApiProperty({ required: false, default: 10 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  limit?: number = 10;

  @ApiProperty({ required: false{{#if searchableFields.length}}, description: 'Search across: {{join searchableFields ", "}}'{{/if}} })
  @IsOptional()
  @IsString()
  search?: string;

  @ApiProperty({ required: false, default: 'id'{{#if sortableFields.length}}, enum: ['id', {{#each sortableFields}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}}]{{/if}} })
  @IsOptional()
  @IsString()
  {{#if sortableFields.length}}
  @IsIn(['id', {{#each sortableFields}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}}])
  {{/if}}
  sort?: string = 'id';

  @ApiProperty({ required: false, enum: ['asc', 'desc'], default: 'desc' })
  @IsOptional()
  @IsString()
  @IsIn(['asc', 'desc'])
  order?: 'asc' | 'desc' = 'desc';
}
